package com.example.recipeman.utils

import android.widget.ListView

class Helper {
    public fun getListViewSize(listView: ListView) {
        val listAdapter = listView.adapter ?: return

        var totalHeight = 0
        listAdapter.count
    }
}